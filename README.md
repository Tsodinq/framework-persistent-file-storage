# Framework Persistent File Storage

This is an incredibly simple server that allows serving of files from NextJS' public directory, since in production mode, dynamically created files cannot be served.